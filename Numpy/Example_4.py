# initialize one-d array with zero

import numpy as np
first=np.zeros((1,4))     # one row , 4 columns # zeros will initialize array to 0
# first=np.ones((1,4))   # ones will initialize array to 1
print(first)
print("\n")

# initialize two-d array with zeros

second=np.zeros((3,4))       # 3 rows and 4 columns

print(second)